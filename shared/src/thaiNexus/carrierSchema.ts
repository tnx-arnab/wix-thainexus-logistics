/** Minimal carrier config - all merchant settings live in the Thai Nexus app iframe. */
export const CARRIER_CONFIGURATION_SCHEMA = {
    connection_options: { fields: [] as never[] },
    zone_options: { fields: [] as never[] },
} as const;
